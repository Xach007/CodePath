Enhance and fix the learning platform with the following requirements:

1. LANGUAGE SYSTEM

- The website must support multiple interface languages (at least Russian 🇷🇺 and English 🇬🇧).
- When Russian is selected, ALL interface text changes to Russian, except programming code.
- When English is selected, ALL interface text changes to English.
- Code comments inside code examples should also translate to the selected language.
- Use real flag icons for each language in the language selector.
- Ensure translations work consistently across:
  - navigation
  - lessons
  - quizzes
  - hints
  - notifications
  - profile and settings

2. PROGRAMMING LANGUAGES AND COURSES

- Add multiple programming languages: Python, JavaScript, HTML, CSS, SQL, C++, Java.
- Each language must have at least 20 lessons (more is better).
- Each lesson contains: theory, quiz, coding challenge.
- Display real programming language icons.
- Add smooth navigation between modules and lessons.

3. LESSON EXIT BUTTON

- Add a CLOSE (X) button in each lesson.
- Clicking it returns the user to the lesson list page of the current course.
- Progress is saved automatically and no data is lost.

4. QUIZ FEEDBACK

- When a user selects an answer, immediately show if it is correct or incorrect.
- Correct answers: green highlight + "Correct!" message.
- Incorrect answers: red highlight + explanation.
- Show scores and update progress instantly.

5. USER PROFILE AND SETTINGS

- Add a profile menu accessible near logout.
- Users can:
  - Change nickname (if not taken)
  - Upload avatar
  - Change language
  - Change theme (dark/light)
- Show avatar in header, leaderboard, and profile page.
- Add achievements/medals system:
  - First lesson completed
  - 10 lessons completed
  - First coding challenge solved
  - Streaks (7-day, 30-day)
  - Course completion
- Medals should display with beautiful icons.

6. DARK MODE DEFAULT

- Default theme must be a dark theme (developer-style, soft gradients, glass UI cards).
- Users can toggle to light mode in settings.
- Save user theme preference.

7. ANIMATION

- The intro animation (fullscreen code editor typing code → terminal execution → website appears) must always run whenever the user enters the site from a browser.
- The animation must be fast, cinematic, and smooth.
- Fullscreen editor, blinking cursor, typing effect, terminal output, website reveal.
- Works on desktop, tablet, mobile.

8. UI / UX

- Smooth animations for buttons, progress bars, hover effects.
- Mobile-first responsive design.
- Modern and polished typography.
- Realistic code editor (Monaco) with autocomplete and hints.

9. NAVIGATION

- SPA-like behavior with HTMX or AJAX.
- No full page reloads.
- Lesson unlocking works sequentially.
- User progress is saved automatically.

10. PERFORMANCE

- Smooth animations at 60fps.
- Optimized for browser and mobile.
- Transitions between lessons, quizzes, coding tasks are seamless.

11. EXTRA FEATURES

- XP points, levels, streaks, leaderboard optional.
- Notifications when lesson completed or achievement unlocked.
- User-friendly interface with gamification and visually appealing graphics.

Ensure all features are fully implemented, visually polished, responsive, and functional for all users