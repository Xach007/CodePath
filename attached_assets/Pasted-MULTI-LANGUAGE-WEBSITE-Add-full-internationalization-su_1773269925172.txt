MULTI LANGUAGE WEBSITE

Add full internationalization support.

The platform must support at least two interface languages:

Russian 🇷🇺
English 🇬🇧

Add a language switcher in the header.

Show small flag icons next to the language names.

Example:

🇷🇺 Русский
🇬🇧 English

When the user switches the language:
 • all interface text changes
 • lesson text changes
 • quiz questions change
 • hints change
 • explanations change

Programming code itself should NOT be translated.

However code comments should be translated to the selected language.

Use Django internationalization (i18n).

Remember user language preference.

⸻

PROGRAMMING LANGUAGES

The platform must support multiple programming languages.

Include at least:

Python
JavaScript
HTML
CSS
SQL

Each language must have its own course.

Each course must contain:

minimum 15 lessons
preferably 20–30 lessons

Structure example:

Python Course
Module 1 — Basics
Module 2 — Variables
Module 3 — Conditions
Module 4 — Loops
Module 5 — Functions
Module 6 — Data structures

Each module contains lessons with theory, quiz, and coding tasks.

⸻

SMART CODE EDITOR (LIKE VS CODE)

Use Monaco Editor (VS Code editor).

Features required:
 • syntax highlighting
 • code suggestions
 • autocomplete
 • inline hints
 • error highlighting
 • formatting
 • dark mode

The editor must behave similar to VS Code.

When user types code, suggestions should appear automatically.

Example:

typing “pri” → suggestion “print()”

⸻

RUN CODE BUTTON

Add a button:

▶ Run Code

When clicked:
 • the code executes
 • output appears in a terminal panel below the editor

Create a terminal-style output window like a real console.

⸻

CHECK SOLUTION BUTTON

Add a second button:

✅ Check Solution

When clicked:
 • code is validated against test cases
 • system checks if the task is solved correctly

If correct:
 • lesson completed
 • next lesson unlocked
 • user earns XP

If incorrect:
 • show error message
 • show hint

⸻

HINT SYSTEM

If user fails several times:

show hints automatically.

Hints should guide the user without revealing the full solution.

Example:

Hint 1:
Remember how to use a for loop.

Hint 2:
You need to print numbers from 1 to 10.

Hint 3:
Try using range(1, 11)

⸻

TERMINAL SIMULATION

Create a terminal-like interface under the editor.

It should display:

program output
errors
print results

Example output:

Hello world
10
25

⸻

LESSON CONTENT GENERATION

Generate course content for all programming languages.

Each language must include at least:

15 lessons minimum

Better if:

20–30 lessons per language.

Lessons must include:

theory explanation
examples
quiz
coding challenge

⸻

MOBILE EXPERIENCE

Ensure the code editor works on mobile devices.

Use responsive layout.

The editor should resize properly on phones and tets.
:::—

⚡ Очень советую добавить ещё одну вещь

Чтобы сайт был как настоящее приложение, добавь этот73642”}

APP-LIKE EXPERIENCE

The platform must behave like a modern web application.

Use HTMX or AJAX so pages never fully reload.

Add smooth transitions between lessons.

Add loading animations.

The platform should feel like a mobile learning app similar to Mimo or Dungo.
:::—